meta {
  name: List my appointments
  type: http
  seq: 2
}

get {
  url: {{base_url}}/v1/appointments/
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

docs {
  The calling client's appointments. Filter with ?status=booked (repeatable).
}
