Django==5.2.3
djangorestframework==3.17.0
drf-spectacular==0.28.0
psycopg[binary]==3.2.13
procrastinate[django]==3.8.0
python-decouple==3.8
gunicorn==22.0.0
whitenoise==6.9.0
python-json-logger==3.2.1
# Notification providers (ADR 0007): SDKs added when real sending is wired
# (telnyx, resend) — the WhatsApp Cloud API uses plain HTTP. Until then the
# provider clients log in dev when unconfigured.
